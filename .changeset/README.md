# Changesets

Run `bun run changeset` when a change should appear in the next release notes. Commit the generated changeset file with the related change.

Run `bun run version` to update the repository version and changelog from pending changesets.
